name: Hadlint
 
on:
  pull_request:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Hadolint
      uses: brpaz/hadolint-action@master
